interface:
  display_name: "Interactive Learning"
  short_description: "Learning diagrams and Obsidian widgets"
  default_prompt: "Use $interactive-learning to teach this topic with the best-fit visual or interactive study artifact. Default to artifact-first output, decide roadmap phase count from topic complexity instead of fixed templates, and when appropriate create a local SVG or HTML artifact directly instead of only describing one."
