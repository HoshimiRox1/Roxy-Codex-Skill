---
name: visualize
description: 在 Codex CLI 里为学习/知识梳理类问答自动生成视觉化笔记 — 包括流程图、架构图、时序图、对比表、状态机、学习路径、交互卡片等。默认产出 Obsidian 兼容的 markdown 笔记(所有 SVG 一律用 dataviewjs 块包裹,交互模块也用 dataviewjs),只有在用户明确要求单文件时才输出独立 .html / .svg。**务必在用户讨论概念、学习路线、系统架构、流程步骤、对比分析、数据关系等场景时主动触发本 skill**,哪怕用户没有明说"画一下""可视化""做个图"——只要内容具备空间结构、时序流程、分类对比或可交互演示特征,视觉化就能显著提升笔记质量。凡是涉及 Obsidian 学习笔记、知识卡片、概念图、学习路径、架构梳理、流程整理、状态转移、知识对比等用户意图时,都要优先考虑使用本 skill。
---

# Visualize — Codex CLI 的学习可视化助手

这个 skill 的目标:把 claude.ai 网页里那种"该用图就自动出图、该用交互就自动出交互"的体验搬到 codex cli,并且产出的是**Obsidian 笔记**而不是孤立的博客风网页。

## 核心理念

你(Codex)不是在做"图表生成器",你是在做**学习笔记**。文字是主体,视觉化是嵌入到文字里的"高辨识度模块",帮助读者在扫读时抓住结构。

三个底线:
1. **默认 markdown**,视觉化嵌入其中。不要生成独立 HTML 站点、博客模板或带导航栏的网页。
2. **透明背景**,样式尽量极简,依赖 Obsidian 当前主题的字色/背景。
3. **不过度装饰**。每个视觉块都要承载具体信息(结构、关系、流程、对比),而不是纯装饰性的分隔线、logo、emoji 墙。

## 最重要的品味原则:得意门生 vs 做题家

这个 skill 是从 claude.ai 网页的 visualize 工具移植来的。用户的期望是——你是**得意门生**,不是**做题家**。

**做题家**:看到"学习路线"就套阶段卡片模板,看到"架构"就套分层图。模板是答案。

**得意门生**:先理解内容的**结构本质**(这是序列?层次?关系?对比?状态?),再从设计词汇库里**选择**或**组合**最匹配的形式。模板是参考词汇,不是答案。

具体来说,每次要生成视觉块之前,先问自己三个问题:

1. **这段内容的结构本质是什么?** — 序列、层次、关系、对比、比例、状态、空间?
2. **什么形式最能让读者"一看就懂"?** — 不是最炫的,是最清楚的
3. **用 `patterns.md` 里的模板能覆盖吗?还是要改造/组合/原创?** — 不能覆盖就改造,别硬套

**读 `references/design-thinking.md`**,那里有详细的设计思维框架,以及**顶部的"反模式警告"**——这是 skill 的灵魂,不是附件。

---

## 决策流程

收到用户问题后,依次问自己:

### Step 0 — 需要视觉化吗?

大多数问题不需要,纯文字答案就够。视觉化要**挣到**它的位置——只有在能传达文字难以传达的东西时才用:

**应该视觉化的信号:**
- 空间/结构关系(架构、层级、组件关系)
- 时序/流程(步骤、状态转移、生命周期、事件序列)
- 数据形状(分布、对比、趋势、分类)
- 多维度对比(方案 A vs B vs C 在多个维度上的差异)
- 学习路径(阶段、先后依赖、各阶段目标)
- 可交互演示(计算、参数调节、折叠展开)
- 用户用了"画/看/展示/可视化/流程图/架构图/路线/对比/时序"等词
- 用户描述的本身就是一个视觉产物:"XX 对比表""XX 状态机""XX 注册表单"

**不应该视觉化的信号:**
- 纯定义、概念解释,一两段文字就能说清
- 执行性任务(修 bug、改代码、改配置、写邮件)
- 简单事实问答
- 情绪/闲聊
- 用户明确说"只要文字""不用图""简单点说"

判断标准:这张图是否让读者**更快抓住结构**?如果答案是"差不多",就不要加——加了反而是噪音。

### Step 1 — 输出到哪?

**默认(Obsidian 笔记):**
```
C:\Users\34434\OneDrive\Obsidian\学习笔记\~Codex\
```
文件名:`<主题>-YYYY-MM-DD.md`,主题用中文,下划线或短横线分词。例:`Redis进阶路线-2026-04-17.md`。如果同一天同主题已存在,追加 `-2`, `-3` 后缀。

> ⚠️ 路径写法必须是 `学习笔记\~Codex`(中间有反斜杠),这是一个子目录路径,不是连写的名字。WSL / git-bash 环境下对应 `/mnt/c/Users/34434/OneDrive/Obsidian/学习笔记/~Codex/`。

**仅当用户明确说「导出 html」「生成一个网页」「单独一个 svg 文件」「给我一份独立的图」等话时,切换到:**
```
C:\Users\34434\Desktop\
```
这时按用户要求产出单独的 `.html` 或 `.svg`,文件名同上规则。

> 先 `ls` 或对应探测命令确认路径存在,不存在就让用户确认或创建。先探测 shell 环境(Windows 原生 / git-bash / WSL),用对应的路径形式。

### Step 2 — 用哪种嵌入形式?

**在 Obsidian 笔记(默认路径)里,所有视觉块一律用 dataviewjs 包裹**:

| 类型 | 用什么 | 理由 |
|------|--------|------|
| 静态图(流程/架构/时序/对比/学习路径) | **dataviewjs 块里 `innerHTML` 注入 SVG** | Codex 直接生成的内联 SVG 在 Obsidian 阅读视图里不可靠,用 dataviewjs 注入 DOM 才稳定渲染 |
| 需要交互(点击展开、参数调节、动态过滤) | **dataviewjs 代码块**(命令式 JS) | 动态生成 DOM,能做状态管理 |
| 表格型数据视图(任务清单、笔记索引) | **dataview 查询块**(非 JS) | 简单声明式,性能好 |

**前提**:用户的 Obsidian 装了 Dataview 插件且启用了"JavaScript Queries"。笔记开头加一行提示:
```markdown
> ℹ️ 本笔记的视觉块用 dataviewjs 渲染,需启用 **Dataview 插件** 的 JavaScript Queries。
```

**仅当用户明说「导出 html」「单独 svg」时**,才在桌面产出独立 `.html` / `.svg` 文件——那种情况下 SVG 是文件主体,不需要 dataviewjs 包裹。

详细嵌入细节、边界情况看 `references/embedding-guide.md`。

---

## 输出文件的结构模板

每篇笔记**必须**遵循这个结构(可以按需增减小节,但框架保持):

````markdown
---
created: 2026-04-17
tags: [学习笔记, Codex生成, <主题标签>]
topic: <主题>
---

# <主题>

> <一句话概括这篇笔记在讲什么、读完能获得什么>

> ℹ️ 本笔记的视觉块用 dataviewjs 渲染,需启用 **Dataview 插件** 的 JavaScript Queries。

## 核心问题 / 起点

<用 1-3 句话点出读者来看这篇笔记是想解决什么>

## <主内容段 1>

<文字叙述>

```dataviewjs
const el = dv.el("div", "", { cls: "viz-svg" });
el.innerHTML = `<svg viewBox="..." xmlns="http://www.w3.org/2000/svg" style="width:100%;max-width:780px;height:auto;font-family:system-ui,sans-serif">
  <!-- SVG 内容 -->
</svg>`;
```

## <主内容段 2>

...

## 关键要点

- 要点 1
- 要点 2
- 要点 3

## 延伸思考 / 下一步

<可选:后续可以深入的方向>
````

**绝对不要**做的事:
- 不要生成 `<!DOCTYPE html>` 开头的完整 HTML 文件(除非用户明说要 html)
- 不要生成带导航栏、页脚、博客式版式的笔记(Obsidian 主题已经处理版式)
- 不要**直接在 markdown 里写内联 `<svg>` 标签**——codex 直出的 SVG 在 Obsidian 阅读视图里不可靠,一律用 dataviewjs 包裹
- 不要在 markdown 开头塞大段 CSS `<style>` 块——阅读视图会剥离
- 不要插入外部 CDN 脚本(`<script src="https://...">`)— Obsidian 不执行这些

---

## dataviewjs + SVG 的标准包裹写法

**静态 SVG 的最小骨架:**

````markdown
```dataviewjs
const el = dv.el("div", "", { cls: "viz-svg" });
el.innerHTML = `<svg viewBox="0 0 780 400" xmlns="http://www.w3.org/2000/svg"
  style="width:100%;max-width:780px;height:auto;font-family:system-ui,sans-serif">
  <!-- 内容 -->
</svg>`;
```
````

**关键约束:**
- **SVG 背景透明**:根元素不设 `background`,让 Obsidian 主题背景透出来
- **颜色从 tokens 取**:不要随手写 `fill="#4a7dc4"`,查 `references/style-tokens.md`,保持一篇笔记色系统一
- **尺寸**:用 `viewBox` + `width:100%` + `max-width`,在不同宽度窗口下自适应
- **字体**:`font-family="inherit"` 或 `system-ui,sans-serif`,跟随 Obsidian
- **模板字符串里不要写反引号** `` ` ``;如果 SVG 内容里真有反引号,切换成字符串拼接或 escape

**交互式 dataviewjs 块**(参数调节、折叠展开等)直接写业务逻辑,不需要先注入 SVG。看 `examples/interactive-concept.md`。

**patterns.md 里的 SVG 片段是结构参考(原生 SVG 语法),实际写入笔记时,把它们套进上面这个 dataviewjs 骨架里。** 不要直接复制 patterns.md 的 SVG 段落到笔记——那样在 Obsidian 阅读视图下渲染会出问题。

---

## 风格与色彩

**极简 + 功能色**。每一笔颜色都要承载语义,不是为了"好看"。

从 `references/style-tokens.md` 里挑配色。核心原则:
- 同一篇笔记里**色系要统一**,最多 2 套调色板(主 + 辅)
- 柔和粉彩(pastel)背景 + 饱和度适中的强调色,不要纯黑纯白对比
- 圆角统一 12px
- 卡片之间留白 16-20px
- 文字 `#1F2937`(深灰而非纯黑),辅助文字 `#6B7280`

---

## 工作流程(写一篇笔记时)

1. **先理解**用户问题,判断是否需要视觉化(Step 0)
2. **起草文字内容**——把要讲的东西用纯文字列大纲,确定哪几处会从视觉化中受益
3. **识别结构本质**——每个视觉化点,它的内容是序列?层次?关系?对比?状态?这一步决定形式
4. **选择或设计形式**——先看 `references/design-thinking.md` 的思维框架(尤其顶部反模式警告),再去 `references/patterns.md` 找匹配的模板结构。找不到完全匹配的,就改造/组合/原创
5. **查 tokens**——从 `references/style-tokens.md` 挑色,保持一篇笔记色系统一
6. **套进 dataviewjs 骨架**——SVG 内容放进上面给的标准包裹写法里
7. **写文件**到目标路径(Step 1)
8. **自检**(见下一节)
9. **告诉用户**文件位置和笔记核心结构,不要把整篇笔记正文复述一遍——用户点开文件自己看就行

---

## 自检清单(写完后逐条过)

- [ ] 文件写在 `C:\Users\34434\OneDrive\Obsidian\学习笔记\~Codex\` 下(路径有反斜杠)
- [ ] YAML frontmatter 至少含 `created`, `tags`, `topic`
- [ ] H1 标题下有一句 `>` 引用作为全篇总结
- [ ] 笔记开头有 dataviewjs 提示行(除非笔记里完全没有视觉块)
- [ ] 所有 SVG 都用 dataviewjs 块包裹,**没有**裸露在 markdown 里的 `<svg>` 标签
- [ ] 所有 SVG 根元素有 `viewBox` 和 `xmlns`,背景透明
- [ ] 所有颜色来自 tokens(没有随手写的色号)
- [ ] **检查相邻 text 元素的 y 坐标差是否 ≥ 行高(通常 ≥16px 给 13px 字号、≥14px 给 11px 字号),避免重叠**
- [ ] **检查文字是否超出卡片边界**(text 的 x + 估算宽度是否小于 rect 的右边界)
- [ ] 没有外部 CDN `<script src>`
- [ ] 没有在 markdown 顶部写 `<style>` 块
- [ ] 文字为主,视觉块只在需要时插入,不是每段都配图
- [ ] 整篇色系统一(2-3 个色相,每个色相有明确语义)

---

## 示例索引

`examples/` 目录下有 5 个完整示例,**特意选择了 5 种不同的设计语言**,让你看到可视化的形式谱系——不是每个问题都该套"阶段卡片":

- `learning-path.md` — **阶段卡片堆叠**:用于"顺序推进的学习路线"
- `architecture-diagram.md` — **分层 + 连线**:用于"系统架构"
- `interactive-concept.md` — **dataviewjs 滑块 + 条形**:用于"参数探索类的交互概念"
- `timeline-evolution.md` — **水平时间线 + 事件卡片**:用于"历史演进、版本迭代"
- `concept-relations.md` — **维恩图 + 决策树**:用于"概念间的抽象关系、权衡取舍"

这五种设计语言有**完全不同的空间组织**。看完这几个示例你应该意识到:同样是"视觉化",形式差异巨大,选哪种完全取决于内容的结构本质。

第一次处理某一类需求时,找结构最接近的示例参考——但**不要直接复制**,理解它为什么这么设计,然后按你的内容调整。

---

## 反模式(做了就说明走偏了)

| 症状 | 说明 | 修正 |
|------|------|------|
| 生成了一个 index.html + style.css + script.js 的小网站 | 走成博客风了 | 改成单个 .md,内容内嵌 |
| 直接在 markdown 里写 `<svg>` 标签 | 阅读视图渲染不可靠 | 全部用 dataviewjs 块包裹 |
| SVG 里把颜色都写死 `fill="#123456"` | 难维护,色系也容易乱 | 查 tokens,用命名色 |
| dataviewjs 里塞了几百行逻辑还没测 | 过度复杂 | 拆成更小的块,或者退回到静态 SVG(仍包在 dataviewjs 里) |
| 笔记开头一大段 `<style>` | Obsidian 会剥离 | 样式内联到元素上 |
| 图片比文字还多 | 本末倒置 | 文字为主,图是补强 |
| 用了渐变、阴影、玻璃态、霓虹色 | 花哨但不增信息量 | 回归纯色 + 细边框 |
| 给每段都配了图 | 过度视觉化 | 只给真正需要视觉化的段落配图 |
| SVG 里相邻文字挤在一起看不清 | 坐标没算 | 自检清单里的 y 坐标检查要过 |
| 看到"学习/进阶/流程"就默认套阶段卡片 | 没识别结构本质 | 读 `design-thinking.md` 顶部的反模式警告 |

---

## 最后

这个 skill 的精神是:**让读者在 Obsidian 里扫读时,视觉块能像路标一样引导注意力**。所有设计决策——色系、布局、透明背景、dataviewjs 嵌入方式——都服务于这个目标。

有疑惑时,去读 `references/` 和 `examples/`,那里有具体的怎么做。
